(function(){const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));const price=v=>{const n=Number(v);return Number.isFinite(n)&&n>0?n.toLocaleString('vi-VN')+' đ':'Thỏa thuận'};async function user(){return(await supabaseClient.auth.getUser()).data?.user||null}async function load(){const box=document.getElementById('productDetail'),id=new URLSearchParams(location.search).get('id');if(!box)return;if(!id){box.innerHTML='<div class="empty-state"><h3>Không tìm thấy tin</h3><a href="index.html">← Về trang chủ</a></div>';return}box.innerHTML='<div class="loading">Đang tải thông tin sản phẩm...</div>';const [{data:item,error},{data:viewer}]=await Promise.all([supabaseClient.from('listings').select('*').eq('id',id).eq('status','approved').maybeSingle(),supabaseClient.auth.getUser()]);if(error||!item){box.innerHTML='<div class="empty-state"><h3>Tin đăng không tồn tại hoặc đã được ẩn</h3><a href="index.html">← Về trang chủ</a></div>';return}const [{data:images},{data:seller}]=await Promise.all([supabaseClient.from('listing_images').select('image_url,sort_order').eq('listing_id',id).order('sort_order'),supabaseClient.from('profiles').select('id,full_name,phone,shop_name,shop_description,avatar_url,verified_at').eq('id',item.seller_id).maybeSingle()]);const gallery=images||[],hero=gallery[0]?.image_url||'',phone=item.contact_phone||seller?.phone||'',zalo=item.contact_zalo||'',logged=!!viewer?.user,safePhone=phone.replace(/[^0-9+]/g,'');let comments=(await supabaseClient.from('listing_comments').select('id,user_id,body,created_at').eq('listing_id',item.id).order('created_at',{ascending:false}).limit(50)).data||[];let reviews=(await supabaseClient.from('reviews').select('rating,body,created_at,reviewer_id').eq('listing_id',item.id).eq('status','approved').order('created_at',{ascending:false}).limit(20)).data||[];const avg=reviews.length?reviews.reduce((s,r)=>s+Number(r.rating),0)/reviews.length:0;box.innerHTML=`<div class="product-detail-shell"><div class="detail-breadcrumb"><a href="index.html">Trang chủ</a><span>›</span><span>${esc(item.category||item.network||'Khác')}</span><span>›</span><strong>${esc(item.title)}</strong></div><div class="detail-layout"><section class="detail-card detail-gallery-card"><div class="detail-hero-wrap">${hero?`<img id="detailHero" class="detail-hero-image" src="${esc(hero)}" alt="${esc(item.title)}">`:'<div class="detail-image-empty">📷<span>Người bán chưa tải ảnh</span></div>'}</div>${gallery.length>1?`<div class="detail-thumbs">${gallery.map((x,i)=>`<button class="detail-thumb ${i?'':'active'}" type="button" data-img="${esc(x.image_url)}"><img src="${esc(x.image_url)}" alt="Ảnh ${i+1}"></button>`).join('')}</div>`:''}</section><section class="detail-card detail-main-card"><div class="detail-category">${esc(item.category||item.network||'Khác')}${item.subcategory?' · '+esc(item.subcategory):''}</div><h1>${esc(item.title)}</h1><div class="detail-price-large">${price(item.price)}</div><div class="detail-location-line">📍 ${esc(item.location||'Phú Thọ')}</div><div class="detail-time-line">🕒 ${item.created_at?new Date(item.created_at).toLocaleString('vi-VN'):'—'} · 👁 ${Number(item.view_count||0)} lượt xem</div><div class="detail-actions-grid">${logged&&phone?`<a class="detail-action primary" href="tel:${safePhone}">☎ Gọi điện</a>`:'<a class="detail-action primary" href="dang-nhap.html">🔒 Đăng nhập để xem số điện thoại</a>'}${zalo?`<a class="detail-action zalo" href="${/^https?:\\/\\//i.test(zalo)?esc(zalo):'https://chat.zalo.me/'+encodeURIComponent(zalo.replace(/\\s/g,''))}" target="_blank" rel="noopener">💬 Zalo</a>`:'<span class="detail-action disabled">💬 Zalo</span>'}<a class="detail-action" href="chat.html?user=${encodeURIComponent(item.seller_id)}&listing=${encodeURIComponent(item.id)}">💬 Nhắn tin</a><button class="detail-action" id="favoriteDetail">♡ Lưu tin</button><button class="detail-action" id="reportDetail">⚑ Báo tin</button></div><div class="detail-section"><h2>Mô tả chi tiết</h2><div class="detail-description">${esc(item.description||'Người bán chưa thêm mô tả.')}</div></div><div class="detail-section"><h2>Thông tin người bán</h2><div class="seller-detail-box"><div class="seller-avatar">${seller?.avatar_url?`<img src="${esc(seller.avatar_url)}" alt="">`:'👤'}</div><div class="seller-detail-copy"><strong>${esc(seller?.shop_name||seller?.full_name||'Người bán')}</strong>${seller?.verified_at?'<span class="seller-verified">✓ Đã xác minh</span>':''}${seller?.shop_description?`<p class="muted">${esc(seller.shop_description)}</p>`:''}<a class="outline-button detail-action" href="nguoi-ban.html?id=${encodeURIComponent(item.seller_id)}">Xem trang người bán</a></div></div></div></section></div><section class="detail-card detail-community"><div class="community-columns"><div><h2>💬 Bình luận</h2><div id="commentsList">${comments.length?comments.map(c=>`<div class="comment-item"><strong>Người dùng</strong><p>${esc(c.body)}</p><small>${new Date(c.created_at).toLocaleString('vi-VN')}</small></div>`).join(''):'<p class="muted">Chưa có bình luận.</p>'}</div>${logged?'<form id="commentForm" class="inline-form"><input id="commentInput" maxlength="500" placeholder="Viết bình luận..."><button>Gửi</button></form>':'<p><a href="dang-nhap.html">Đăng nhập</a> để bình luận.</p>'}</div><div><h2>⭐ Đánh giá người bán ${avg?`(${avg.toFixed(1)}/5)`:''}</h2><div>${reviews.length?reviews.map(r=>`<div class="comment-item"><strong>${'★'.repeat(Number(r.rating))}${'☆'.repeat(5-Number(r.rating))}</strong><p>${esc(r.body||'Đánh giá giao dịch')}</p><small>${new Date(r.created_at).toLocaleDateString('vi-VN')}</small></div>`).join(''):'<p class="muted">Chưa có đánh giá.</p>'}</div>${logged?'<form id="reviewForm" class="review-form"><select id="reviewRating"><option value="5">5 sao</option><option value="4">4 sao</option><option value="3">3 sao</option><option value="2">2 sao</option><option value="1">1 sao</option></select><input id="reviewBody" maxlength="500" placeholder="Chia sẻ trải nghiệm"><button>Đánh giá</button></form>':''}</div></div></section></div>`;document.querySelectorAll('.detail-thumb').forEach(b=>b.onclick=()=>{document.getElementById('detailHero').src=b.dataset.img;document.querySelectorAll('.detail-thumb').forEach(x=>x.classList.remove('active'));b.classList.add('active')});const fav=document.getElementById('favoriteDetail');if(fav)fav.onclick=async()=>{const u=await user();if(!u)return location.href='dang-nhap.html';const q=await supabaseClient.from('favorites').select('id').eq('listing_id',item.id).eq('user_id',u.id).maybeSingle();if(q.data){await supabaseClient.from('favorites').delete().eq('id',q.data.id);fav.textContent='♡ Lưu tin'}else{const r=await supabaseClient.from('favorites').insert({listing_id:item.id,user_id:u.id});if(r.error)return alert('Không thể lưu tin.');fav.textContent='♥ Đã lưu'}};document.getElementById('reportDetail').onclick=async()=>{const u=await user();if(!u)return location.href='dang-nhap.html';const reason=prompt('Lý do báo tin?');if(!reason)return;const r=await supabaseClient.from('listing_reports').insert({listing_id:item.id,reporter_id:u.id,reason,details:''});alert(r.error?'Không thể gửi báo tin.':'Đã gửi báo tin.');};const cf=document.getElementById('commentForm');if(cf)cf.onsubmit=async e=>{e.preventDefault();const u=await user(),body=document.getElementById('commentInput').value.trim();if(!body)return;const r=await supabaseClient.from('listing_comments').insert({listing_id:item.id,user_id:u.id,body});if(r.error)alert('Không thể đăng bình luận: '+r.error.message);else load()};const rf=document.getElementById('reviewForm');if(rf)rf.onsubmit=async e=>{e.preventDefault();const u=await user(),rating=Number(document.getElementById('reviewRating').value),body=document.getElementById('reviewBody').value.trim();if(!body)return;const r=await supabaseClient.from('reviews').insert({listing_id:item.id,reviewer_id:u.id,seller_id:item.seller_id,rating,body,status:'pending'});if(r.error)alert('Không thể gửi đánh giá: '+r.error.message);else{alert('Đã gửi đánh giá, chờ duyệt.');rf.reset()}}}
document.addEventListener('DOMContentLoaded',()=>typeof supabaseClient!=='undefined'&&load());})();